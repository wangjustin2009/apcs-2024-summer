#include <stdio.h>
//從鍵盤上輸入10個數，求其平均值

int main()
{
    int a,sum;
    for (int i=0;i<10;i++){
         printf("輸入%d個數:",i+1);
         scanf("%d",&a);
         sum+=a;  //sum=sum+a
    }
    int g;
    g=sum/10;
    printf("平均值:%d", g);
    return 0;
}
